class ProjectManager:
    """
    Generates and manages tasks.
    Receives a goal and is responsible for generating tasks that will help.
    The tasks are then sent to agents with other roles (e.g., programmer) for execution.
    """
